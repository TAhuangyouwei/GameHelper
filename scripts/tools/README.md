# 维护中工具

本目录按用途保存继续维护的独立工具：

- `animation/`：动画处理、范围转换和轨迹工具。
- `export/`：FBX 导出、文件查看与合并工具。
- `facial/`：面部绑定工具及必须同目录加载的依赖。
- `rigging/`：身体绑定、骨骼拆分和规范检查工具。
- `skin/`：蒙皮、权重和 Pose 工具。
- `scene/`：场景对象、摄像机、顶点色和 Bake 工具。

由主面板打开的脚本路径统一登记在 `../modules/CGH_ToolRegistry.ms`。
